# --- core engine (offline, zero-config) ---
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
rapidfuzz>=3.9
XlsxWriter>=3.2

# --- demo UI ---
streamlit>=1.37

# --- optional hosted-model layer (NVIDIA NIM, OpenAI-compatible).
# Never imported unless the proposal layer is explicitly enabled, so the app
# deploys and runs fine without a key.
openai>=1.40
