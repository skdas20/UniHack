# Optional: the distilled local-model layer (glassbox/distill.py).
# Install these ONLY to run `python run.py --models` or the Streamlit toggle.
# The core engine (plain `python run.py`) needs none of them.
#
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
#   python -m pip install -r requirements-ml.txt
#
# Trained encoders live in training/models/{classpath,spans}; retrain with
# training/eval_models.py --retrain. accelerate is training-only.
torch>=2.3
transformers>=4.44
accelerate>=0.30
pytest>=8.0  # test suite
